# 抖音直播弹幕采集工具 - 打包说明

## 🚀 解决Node.js依赖问题

### 问题描述
原程序需要通过Node.js环境调用sign.js文件生成签名，但打包后的exe文件无法访问Node.js环境。

### 解决方案
程序现在使用**多层级签名生成策略**：

1. **优先使用py_mini_racer**：内置JavaScript引擎，无需Node.js
2. **备用Node.js执行**：如果系统有Node.js则使用
3. **内置备用算法**：完全不依赖外部环境的签名生成

## 方法一：使用一键打包脚本（推荐）

### Windows批处理脚本
直接双击运行 `build.bat` 文件，会自动完成所有步骤。

### Python打包脚本
运行 `python build_exe.py`，按提示选择操作。

## 方法二：手动打包

### 1. 安装PyInstaller
```bash
pip install pyinstaller
```

### 2. 安装所有依赖（重要）
```bash
pip install pyinstaller>=5.0
pip install openpyxl>=3.0.0
pip install websocket-client>=1.6.0
pip install py-mini-racer>=0.6.0  # 🔥 关键依赖
pip install requests>=2.25.0
pip install certifi>=2021.5.25
```

### 3. 打包命令

#### 单文件版本（推荐）
```bash
pyinstaller --name="抖音直播弹幕采集工具" --onefile --windowed --add-data="protobuf;protobuf" --add-data="sign.js;." --hidden-import=openpyxl --hidden-import=websocket --hidden-import=py_mini_racer --hidden-import=py_mini_racer.py_mini_racer --collect-all=py_mini_racer ui_main.py
```

## 📋 关键改进

### 1. JavaScript引擎选择
- **py_mini_racer**：内置V8引擎，无需外部依赖
- **Node.js**：如果系统安装了Node.js则作为备用
- **备用算法**：纯Python实现的签名生成

### 2. 打包参数优化
```bash
--hidden-import=py_mini_racer           # 导入py_mini_racer主模块
--hidden-import=py_mini_racer.py_mini_racer  # 导入核心模块
--collect-all=py_mini_racer             # 收集所有相关文件
```

### 3. 容错机制
- 多种签名生成方式，确保程序稳定运行
- 详细的错误日志，便于问题排查
- 自动降级到备用方案

## 🔧 环境要求

### 开发环境
- Python 3.7+
- py_mini_racer >= 0.6.0

### 运行环境
- Windows 7/10/11
- 无需Node.js（已内置JavaScript引擎）
- 无需Python环境（打包后独立运行）

## 📊 输出文件

打包完成后，在 `dist` 目录下会生成：
- 单文件版本: `抖音直播弹幕采集工具.exe`
- 目录版本: `抖音直播弹幕采集工具/` 文件夹

## ⚠️ 注意事项

### 1. 依赖检查
确保安装了所有必要的Python包，特别是：
```bash
pip install py-mini-racer>=0.6.0
```

### 2. 文件完整性
确保这些文件存在：
- `protobuf/` 目录
- `sign.js` 文件

### 3. 签名机制
- 程序会自动选择最佳的签名生成方式
- 在控制台中会显示使用的签名方法
- 如果所有方法都失败，会使用最基础的备用算法

### 4. 性能对比
- **py_mini_racer**: 快速，稳定，推荐
- **Node.js**: 较快，需要外部环境
- **备用算法**: 最慢，但100%兼容

## 🐛 常见问题

### 1. py_mini_racer安装失败
```bash
# Windows用户可能需要安装Visual C++构建工具
pip install --upgrade pip
pip install py-mini-racer
```

### 2. 打包体积过大
py_mini_racer会增加约20-30MB的文件大小，这是正常的。

### 3. 签名验证失败
程序会自动尝试多种签名方式，通常不会影响正常使用。

### 4. 运行时错误
检查控制台输出，程序会显示使用的签名方法和详细错误信息。

## 🎯 测试建议

1. **开发环境测试**: 确保程序在有Node.js和无Node.js环境下都能运行
2. **打包测试**: 在干净的Windows系统上测试打包后的exe文件
3. **功能测试**: 验证WebSocket连接和数据采集功能正常

## 📈 版本历史

- **v1.2**: 集成py_mini_racer，解决Node.js依赖问题
- **v1.1**: 优化UI界面，添加现代化设计
- **v1.0**: 基础功能实现
